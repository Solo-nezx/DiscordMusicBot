[phases.setup]
nixPkgs = ["python313", "ffmpeg", "nodejs_20"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "npm install -g @imputnet/bgutil-ytdlp-pot-provider"
]

[start]
cmd = "bash start.sh"
